# KWASNY LOG MANAGER - System Architecture

## 🏗️ System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     KWASNY LOG MANAGER                          │
│                   Multi-Account Management System                │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACES                           │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ GUI Panel    │  │ CLI Tool     │  │ Python API   │          │
│  │ (PyQt6)      │  │ (cli.py)     │  │ (src.main)   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    MAIN COORDINATOR                              │
│                  (KwasnyLogManager)                              │
│                                                                  │
│  • Orchestrates all modules                                     │
│  • Manages account workflows                                    │
│  • Coordinates parallel processing                              │
└─────────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
┌───────────────────┐ ┌──────────────┐ ┌──────────────┐
│  CONFIGURATION    │ │   DATABASE   │ │    MODULES   │
│     SYSTEM        │ │   (SQLite)   │ │   (Core)     │
└───────────────────┘ └──────────────┘ └──────────────┘

```

## 📦 Module Architecture

### 1. Proxy Management Layer
```
┌─────────────────────────────────────────────┐
│         PROXY MANAGER MODULE                │
├─────────────────────────────────────────────┤
│  • Parse proxy URLs (SOCKS5/HTTP/HTTPS)    │
│  • Test proxy connectivity                  │
│  • Assign unique proxy per account          │
│  • Blacklist failed proxies                 │
│  • Automatic rotation on failure            │
└─────────────────────────────────────────────┘
         │
         │ provides proxy config
         ▼
   [Each Account] ───→ [Unique Proxy] ───→ [Internet]
```

### 2. Automation Layer
```
┌─────────────────────────────────────────────┐
│      EMAIL AUTOMATION MODULE                │
├─────────────────────────────────────────────┤
│  • Auto-detect provider (wp.pl, onet.pl...) │
│  • Navigate to login URL                    │
│  • Fill credentials                         │
│  • Verify login status                      │
│  • Extract verification codes               │
└─────────────────────────────────────────────┘
         │
         │ uses
         ▼
┌─────────────────────────────────────────────┐
│     FACEBOOK AUTOMATION MODULE              │
├─────────────────────────────────────────────┤
│  • Login to Facebook                        │
│  • Detect login status                      │
│  • Auto-reset password via email            │
│  • Handle checkpoints and 2FA               │
└─────────────────────────────────────────────┘
         │
         │ protected by
         ▼
┌─────────────────────────────────────────────┐
│      SECURITY MANAGER MODULE                │
├─────────────────────────────────────────────┤
│  • Scan for active sessions                 │
│  • Logout unauthorized sessions             │
│  • Monitor for incoming calls               │
│  • Auto-reject calls                        │
│  • Daily security scans                     │
└─────────────────────────────────────────────┘
```

### 3. Financial & Analytics Layer
```
┌─────────────────────────────────────────────┐
│    FINANCIAL CALCULATOR MODULE              │
├─────────────────────────────────────────────┤
│  • Track costs (proxy, account, email)     │
│  • Track revenues and activity              │
│  • Calculate daily/total profit             │
│  • Compute ROI                              │
│  • Generate financial reports               │
└─────────────────────────────────────────────┘
```

## 🔄 Workflow Diagram

### Single Account Processing Flow
```
START
  │
  ▼
┌─────────────────┐
│ Get Account     │
│ Details from DB │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Get Assigned    │
│ Proxy Config    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     SUCCESS
│ Login to Email  ├──────────┐
│ (with Proxy)    │          │
└────────┬────────┘          │
         │ FAIL              │
         ▼                   ▼
   [Try Proxy           ┌─────────────────┐
    Rotation]           │ Login to        │
         │              │ Facebook        │
         │              └────────┬────────┘
         │                       │
         │              ┌────────┴────────┐
         │              │ WRONG_PASSWORD? │
         │              └────────┬────────┘
         │                  YES  │  NO
         │                   ▼   │
         │              ┌──────────────┐
         │              │ Auto-Reset   │
         │              │ Password via │
         │              │ Email        │
         │              └──────┬───────┘
         │                     │
         └─────────────────────┼────────┐
                               │        │
                               ▼        ▼
                         ┌──────────────────┐
                         │ Security Scan:   │
                         │ • Logout Sessions│
                         │ • Monitor Calls  │
                         └────────┬─────────┘
                                  │
                                  ▼
                         ┌──────────────────┐
                         │ Update Financial │
                         │ Data in DB       │
                         └────────┬─────────┘
                                  │
                                  ▼
                         ┌──────────────────┐
                         │ Log Results      │
                         └────────┬─────────┘
                                  │
                                  ▼
                                END
```

## 🗄️ Database Schema

```
┌─────────────────────────────────────────────────────────┐
│                    ACCOUNTS                             │
├─────────────────────────────────────────────────────────┤
│ id (PK)                                                 │
│ email                                                   │
│ email_password                                          │
│ facebook_email                                          │
│ facebook_password                                       │
│ proxy_id (FK → proxies)                                │
│ email_status                                            │
│ facebook_status                                         │
│ created_at, last_check, active                         │
└─────────────────────────────────────────────────────────┘
                        │
                        │ 1:1
                        ▼
┌─────────────────────────────────────────────────────────┐
│                    PROXIES                              │
├─────────────────────────────────────────────────────────┤
│ id (PK)                                                 │
│ proxy_url                                               │
│ proxy_type                                              │
│ last_test                                               │
│ is_working                                              │
│ blacklisted                                             │
│ failure_count                                           │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                 FINANCIAL_DATA                          │
├─────────────────────────────────────────────────────────┤
│ id (PK)                                                 │
│ account_id (FK → accounts)                             │
│ date                                                    │
│ proxy_cost, account_cost_daily, email_cost_daily       │
│ operational_cost, daily_revenue                         │
│ activity_percentage, calculated_profit                  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                      LOGS                               │
├─────────────────────────────────────────────────────────┤
│ id (PK)                                                 │
│ account_id (FK → accounts)                             │
│ timestamp, action, status                               │
│ details, proxy_used, duration                           │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                 SECURITY_EVENTS                         │
├─────────────────────────────────────────────────────────┤
│ id (PK)                                                 │
│ account_id (FK → accounts)                             │
│ timestamp, event_type                                   │
│ sessions_logged_out, calls_rejected                     │
└─────────────────────────────────────────────────────────┘
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│              ACCOUNT ISOLATION LAYERS                   │
└─────────────────────────────────────────────────────────┘

Account 1                  Account 2                  Account N
    │                          │                          │
    ├─ Unique Proxy 1          ├─ Unique Proxy 2         ├─ Unique Proxy N
    ├─ Separate Browser        ├─ Separate Browser       ├─ Separate Browser
    ├─ Random User-Agent       ├─ Random User-Agent      ├─ Random User-Agent
    ├─ Unique Viewport         ├─ Unique Viewport        ├─ Unique Viewport
    ├─ Random Delays           ├─ Random Delays          ├─ Random Delays
    └─ Isolated Process        └─ Isolated Process       └─ Isolated Process
         │                          │                          │
         └──────────────────────────┴──────────────────────────┘
                                    │
                              NO SHARING
```

### Anti-Detection Measures
```
┌────────────────────────────────────────────────┐
│ Browser Fingerprinting Protection             │
├────────────────────────────────────────────────┤
│ • Randomized User-Agent per account           │
│ • Variable viewport sizes (1920x1080 base)    │
│ • Locale matching (pl-PL)                     │
│ • Timezone alignment with proxy location      │
│ • Cookie isolation per session                │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│ Behavioral Anonymization                       │
├────────────────────────────────────────────────┤
│ • Random delays between actions (0.5-3s)      │
│ • Human-like typing patterns                   │
│ • Natural mouse movements                      │
│ • Varied operation timing                      │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│ Network Isolation                              │
├────────────────────────────────────────────────┤
│ • One proxy = One account (strict 1:1)        │
│ • No IP address sharing                        │
│ • Independent session management               │
│ • Separate process per account                 │
└────────────────────────────────────────────────┘
```

## 📊 Financial Calculation Formula

```
DAILY_COST = 
    proxy_cost_daily + 
    (account_cost_total / 30) +      # Amortization
    (email_cost_total / 30) +        # Amortization
    operational_cost_daily

ACTUAL_REVENUE = 
    daily_revenue × (activity_percentage / 100)

DAILY_PROFIT = 
    ACTUAL_REVENUE - DAILY_COST

TOTAL_PROFIT = 
    Σ (DAILY_PROFIT × days_active)

ROI = 
    (TOTAL_PROFIT / TOTAL_INVESTMENT) × 100
```

### Example Calculation
```
Given:
  proxy_cost_daily = $0.15
  account_cost_total = $3.00 (amortized over 30 days = $0.10/day)
  email_cost_total = $0.30 (amortized over 30 days = $0.01/day)
  operational_cost = $0.05
  daily_revenue = $1.50
  activity = 85%

Calculation:
  DAILY_COST = $0.15 + $0.10 + $0.01 + $0.05 = $0.31
  ACTUAL_REVENUE = $1.50 × 0.85 = $1.275
  DAILY_PROFIT = $1.275 - $0.31 = $0.965
  
  After 15 days:
  TOTAL_PROFIT = $0.965 × 15 = $14.475
```

## 🎯 Key Features Summary

### ✅ Implemented
1. **Proxy System** - Complete isolation, testing, rotation
2. **Email Automation** - 5 providers supported, auto-detection
3. **Facebook Automation** - Login, auto-reset, status tracking
4. **Security Management** - Session logout, call rejection
5. **Financial Tracking** - Costs, revenues, profit calculation
6. **Admin Panel (GUI)** - Full-featured PyQt6 interface
7. **Logging System** - Comprehensive activity tracking
8. **Configuration** - INI/JSON/TXT config files
9. **Anti-Detection** - Randomization, isolation, anonymity
10. **CLI Interface** - Command-line tool for operations

### 📂 File Structure
```
KwasnySzpontMenager/
├── src/
│   ├── database.py               # SQLite database management
│   ├── main.py                   # Main coordinator
│   ├── modules/
│   │   ├── proxy_manager.py      # Module 1: Proxy system
│   │   ├── email_automation.py   # Module 2: Email automation
│   │   ├── facebook_automation.py # Module 3: Facebook automation
│   │   ├── security_manager.py   # Module 4: Security management
│   │   └── financial_calculator.py # Module 5: Financial stats
│   ├── gui/
│   │   └── admin_panel.py        # Module 6: Admin panel GUI
│   └── utils/
│       └── config_loader.py      # Module 8: Configuration
├── config/                        # Configuration files
├── cli.py                        # CLI interface
├── test_system.py                # System tests
├── setup.sh / setup.bat          # Setup scripts
├── README.md                     # Full documentation
└── QUICKSTART.md                 # Quick start guide
```

## 🚀 Usage Patterns

### Pattern 1: Batch Processing
```python
# Process all accounts nightly
manager = KwasnyLogManager()
await manager.process_all_accounts(parallel=True)
```

### Pattern 2: Single Account Management
```python
# Process specific account
manager = KwasnyLogManager()
result = await manager.process_single_account(account_id=1)
```

### Pattern 3: Financial Analysis
```python
# Get global statistics
manager = KwasnyLogManager()
stats = manager.financial_calculator.get_global_statistics()
print(f"Total profit: ${stats['total_profit']:.2f}")
```

### Pattern 4: GUI Operation
```python
# Launch interactive panel
from src.gui.admin_panel import main
main()
```

## 🎓 Best Practices

1. **Isolation First**: Always use unique proxy per account
2. **Monitor Regularly**: Check logs and statistics daily
3. **Backup Data**: Regularly backup database file
4. **Secure Configs**: Never commit sensitive config files
5. **Test Proxies**: Verify proxy functionality before use
6. **Schedule Wisely**: Run operations during off-peak hours
7. **Track Finances**: Monitor ROI and profitability
8. **Security Scans**: Run daily security checks
9. **Error Handling**: Review failed operations promptly
10. **Scale Gradually**: Start small, scale up after testing

---

**System Status**: ✅ Fully Implemented and Operational
**Version**: 1.0.0
**Last Updated**: 2026-02-02
